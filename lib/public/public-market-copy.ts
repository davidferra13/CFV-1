import { PUBLIC_MARKET_SCOPE } from '@/lib/public/public-market-scope'

export const PUBLIC_REQUEST_ROUTING_COPY = `ChefFlow accepts requests across the ${PUBLIC_MARKET_SCOPE}.`

export const PUBLIC_DIRECTORY_LIVE_COVERAGE_COPY =
  'Directory coverage reflects chefs and venues that are live now, not blanket availability in every market.'

export const PUBLIC_MARKET_POSITIONING_COPY = `ChefFlow is positioned as a national platform for ${PUBLIC_MARKET_SCOPE}, while live directory availability follows each chef's real service area, travel range, and venue footprint.`

export const PUBLIC_MATCHING_EXPLAINER_COPY =
  'Describe your event once. ChefFlow matches by location, service type, and guest count before sharing it with chefs.'

export const PUBLIC_MATCHING_FOLLOWUP_COPY =
  'Open requests are capped at 10 matched chefs. If no chef matches yet, ChefFlow can save the request and follow up as live coverage expands.'

export const PUBLIC_MATCHING_SCOPE_COPY = `${PUBLIC_MATCHING_EXPLAINER_COPY} ${PUBLIC_REQUEST_ROUTING_COPY}`
