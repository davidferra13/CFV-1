// Stub: module was removed but still imported in 5 files.
// TODO: clean up dangling imports in remy-context, remy-public-context, inquiry-first-response, chef-profile-readiness, circle-chef-proof

export interface ChefServiceConfig {
  [key: string]: unknown
}

export function formatServiceConfigForPrompt(_config: ChefServiceConfig | null): string {
  return ''
}
