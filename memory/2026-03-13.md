# 2026-03-13

## Work Log

- Continued Priority 1 after midnight with another recipe-library usability pass on the canonical `/recipes` surface.
- Added quick filters for menu-ready recipes, priced recipes, and recipes still missing operations details so chefs can triage the library faster while building menus.
- Updated the recipe library empty state to distinguish between an empty book and an over-filtered result set.
- Verified the recipe-library quick-filter pass with targeted eslint on the changed file.
- Added matching quick filters to the canonical ingredient library for currently priced ingredients, staples, and accommodation-aware ingredients.
- Updated the ingredient library empty state to distinguish between an empty table and an over-filtered result set, then verified the pass with targeted eslint.
- Added ingredient-library sort controls for alphabetical order, most-used ingredients, and highest current price signals.
- Verified the ingredient-library sorting pass with targeted eslint on the changed page and client files.
- Added menu-library quick filters for costed menus, templates, and menus still missing guest count so builder triage is faster on the canonical `/menus` page.
- Verified the menu-library quick-filter pass with targeted eslint on the changed client file.
- Added top-level menu-library stats for total menus, active menus, costed menus, and menus still missing guest count.
- Verified the menu-library coverage-stats pass with targeted eslint on the changed client file.
- Added builder-readiness alerts on menu cards for menus still missing guest count or food-cost coverage.
- Verified the menu-card readiness pass with targeted eslint on the changed client file.
- Improved the menu-library empty state so it distinguishes between an empty menu library and a filtered zero-result state.
- Added builder-aware empty-state guidance for costed, template, and needs-guest-count filters, then verified the pass with targeted eslint.
- Added a highest-food-cost sort option to the canonical menu library so expensive menus can be reviewed first.
- Verified the menu-library food-cost sorting pass with targeted eslint on the changed client file.
- Added a matching highest-food-cost sort option to the canonical recipe library so expensive recipes can be reviewed first before menu assembly.
- Updated the living app audit, wrote a follow-up note for the recipe-library cost-sorting pass, and verified the change with targeted eslint on the changed recipe client file.
- Added a `Needs cost data` quick filter to the canonical recipe library so uncosted recipes can be isolated before they block menu pricing.
- Expanded the filtered empty state with cost-specific guidance, updated the app audit, wrote a follow-up note, and verified the pass with targeted eslint on the changed recipe client file.
- Reworked the top recipe-library stats so missing cost coverage is visible immediately, including a dedicated `Missing Cost Data` summary card.
- Updated the app audit, wrote a follow-up note for the stats pass, and verified the change with targeted eslint on the changed recipe client file.
- Added a `Has components` quick filter to the canonical recipe library so component-linked recipes can be isolated faster during menu-builder prep.
- Expanded the filtered empty state with component guidance, updated the app audit, wrote a follow-up note, and verified the pass with targeted eslint on the changed recipe client file.
- Expanded the `Needs ops details` empty state with more specific success guidance so ops-ready recipe sets do not look like broken filters.
- Updated the app audit, wrote a follow-up note for the ops empty-state pass, and verified the change with targeted eslint on the changed recipe client file.
- Expanded the `Menu-ready only` empty state with specific readiness guidance so chefs know zero results means timing, yield, or equipment coverage is still missing.
- Updated the app audit, wrote a follow-up note for the menu-ready empty-state pass, and verified the change with targeted eslint on the changed recipe client file.
- Added an explicit active quick-filter chip to the recipe-library summary row so the current review mode is always visible.
- Updated the app audit, wrote a follow-up note for the active-filter chip pass, and verified the change with targeted eslint on the changed recipe client file.
- Added an `in view` summary chip to the recipe-library pill row so the current filtered result size is visible immediately.
- Updated the app audit, wrote a follow-up note for the in-view count pass, and verified the change with targeted eslint on the changed recipe client file.
- Added an explicit active sort chip to the recipe-library summary row so the current ordering is visible beside the active filter state.
- Updated the app audit, wrote a follow-up note for the active-sort chip pass, and verified the change with targeted eslint on the changed recipe client file.
- Made the active quick-filter chip directly clickable so the current recipe-library slice can be cleared from the summary row.
- Updated the app audit, wrote a follow-up note for the clear-filter chip pass, and verified the change with targeted eslint on the changed recipe client file.
- Made the active sort chip directly clickable so the recipe-library ordering can be reset to the default A-Z mode from the summary row.
- Updated the app audit, wrote a follow-up note for the clear-sort chip pass, and verified the change with targeted eslint on the changed recipe client file.
- Removed the duplicate `Clear quick filter` button from the quick-filter row now that the active filter chip itself handles dismissal.
- Updated the app audit, wrote a follow-up note for the clear-filter dedup pass, and verified the change with targeted eslint on the changed recipe client file.
- David issued the canonical ChefFlow Strict AI Policy. Saved as `AI-POLICY.md` at workspace root. Covers: absolute recipe generation ban, formula-over-AI rule, private data stays local (Ollama only), AI assists drafting but never owns truth, banned operations (lifecycle transitions, financial ledger, identity/access, silent automation), zero hallucination rule, no em dashes, and the hard boundary (unplug AI and ChefFlow still works). This policy governs every agent and model in the system, no exceptions.
- Ran full AI policy compliance audit across the codebase. Found enforcement layer already solid: `restricted-actions.ts`, `remy-input-validation.ts`, and `parseWithOllama` all exist and work correctly.
- Identified 8 policy violations (4 HIGH, 2 MEDIUM, 2 LOW) where Gemini cloud was receiving private data.
- Phase 1 fix (HIGH severity): migrated 4 files from Gemini to Ollama:
  - `lib/ai/review-request.ts`: client names, event details, menu items were going to Gemini. Now uses `parseWithOllama`.
  - `lib/ai/testimonial-selection.ts`: client names, feedback text, survey responses were going to Gemini. Now uses `parseWithOllama`.
  - `lib/events/debrief-actions.ts`: client name, occasion, date, guest count, menu were going to Gemini. Now uses `parseWithOllama`.
  - `lib/ai/service-timeline.ts`: dietary restrictions, allergies, special requests, staff names, location were going to Gemini. Now uses `parseWithOllama`.
- Phase 2 (MEDIUM) still pending: `parse-receipt.ts` and `parse-document-vision.ts` use Gemini vision (image analysis). Needs local vision model or architecture decision.
- Phase 3 (LOW) still pending: `recipe-scaling.ts` sends chef recipe content to Gemini. `gemini-service.ts` is dead code (replaced by `ace-ollama.ts`) but still importable.
- David provided the definitive OpenClaw Model Hierarchy document. Saved as `docs/OPENCLAW-HIERARCHY.md`. Key corrections from SOUL.md: Build and QA use Groq Llama 3.3 70B (free cloud), not Ollama 30B models. Runner uses Groq Llama 3.1 8B (free cloud), not Ollama qwen3:4b. Sonnet is the primary code writer (80%+ of implementation), not Build. Updated SOUL.md agent table and delegation rules to match. Updated MEMORY.md with infrastructure details.
