# Auto-Repair: Missing route: app/(chef)/dashboard/page.tsx

## Source

Generated by regression detection system on 2026-04-27
Report: .regression-report.md

## Problem

The route `app/(chef)/dashboard/page.tsx` (group: core) is registered in the regression registry but does not exist on disk. It may have been accidentally deleted, moved, or renamed.

## Fix

1. Check git history for the file: `git log --all --follow -- 'app/(chef)/dashboard/page.tsx'`
2. If moved/renamed: update `scripts/regression-registry.json` to point to the new path
3. If accidentally deleted: restore from git: `git checkout HEAD~1 -- 'app/(chef)/dashboard/page.tsx'` (adjust commit as needed)
4. If intentionally removed: remove from regression registry

## Verification

1. Verify `app/(chef)/dashboard/page.tsx` exists on disk or has been removed from the registry
2. Run `bash scripts/regression-check.sh --quick` and confirm no regression for this file

## Constraints

- Do not delete or remove existing functionality
- Do not modify unrelated code
- Run `bash scripts/regression-check.sh --quick` after fix to verify no new regressions
