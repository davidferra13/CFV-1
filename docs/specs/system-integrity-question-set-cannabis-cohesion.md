# System Integrity Question Set: Cannabis Cross-System Cohesion

> **Purpose:** Expose every failure point where cannabis infrastructure, universal platform systems, and cross-cutting integrations fail to connect into a fully specified, verifiable whole. Cannabis is the stress test; cohesion is the goal.
> **Scope:** 50 questions across 10 domains. Every question cites specific code paths. Questions marked [UNIVERSAL] benefit all users. Questions marked [CANNABIS] are cannabis-specific.
> **Created:** 2026-04-18
> **Investigator:** Claude Opus 4.6 (main session, direct code investigation + MemPalace mining of 20+ conversations)
> **Method:** Source code tracing, MemPalace cross-reference, architecture audit, cross-system dependency mapping
> **Prior audit:** `system-integrity-question-set-cannabis-portal.md` (30.5/40, 76.3%) covers the portal in isolation. This set covers cross-system integration.
> **Complementary to:** `system-integrity-question-set-cross-system-cohesion.md` (44/52, non-cannabis chains)
> **Score:** 25.5 / 50 (51.0%) [was 20.5/50 pre-fix]

---

## A. Inquiry-to-Event Cannabis Pipeline (5 questions)

Cannabis preference enters the system via client emails, Wix forms, or manual inquiry entry. It must survive every transformation without data loss or semantic drift. These pipeline patterns serve ALL inquiry types.

| #   | Question                                                                                                  | Scope       | Verdict     | Evidence                                                                                                                                                                                                                                  |
| --- | --------------------------------------------------------------------------------------------------------- | ----------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | Does `mapCannabisPreferenceToBoolean()` exist in exactly one place?                                       | [UNIVERSAL] | **PASS**    | **FIXED:** Extracted to `lib/inquiries/cannabis-preference.ts`. Both `lib/inquiries/actions.ts` and `lib/gmail/sync.ts` now import from shared module. Single source of truth.                                                            |
| A2  | Does `confirmed_cannabis_preference` survive email extraction all the way to event `cannabis_preference`? | [UNIVERSAL] | **PASS**    | Full pipeline traced through 7 hops: `extractCannabisMentions()` -> `cannabis_mentions[]` -> `confirmed_cannabis_preference` -> `mapCannabisPreferenceToBoolean()` -> `cannabis_preference`. No data loss at any hop.                     |
| A3  | Does `cannabis_preference` carry forward in series materialization? Do overlay tables auto-populate?      | [CANNABIS]  | **PASS**    | `series-materialization.ts:256` writes preference. DB triggers use `FOR EACH ROW` (fires per-row on batch inserts). Overlay tables auto-populate correctly.                                                                               |
| A4  | Does Wix form intake correctly extract `confirmed_cannabis_preference`?                                   | [UNIVERSAL] | **PASS**    | `wix/process.ts:168` writes `confirmed_cannabis_preference` from parsed form output. Correct field mapping.                                                                                                                               |
| A5  | Does the UI pre-populate cannabis toggle when converting inquiry to event?                                | [UNIVERSAL] | **PARTIAL** | Backend conversion works (`actions.ts:1664+1703`). But `/events/new` page ignores `?cannabis` param (not in `searchParams` type). V2 API conversion at `api/v2/inquiries/[id]/convert/route.ts:50-76` drops cannabis preference entirely. |

**Score: 4 PASS, 1 PARTIAL, 0 FAIL = 4.5 / 5**

---

## B. Guest Intake & Consent Universality (5 questions)

The guest RSVP portal's cannabis intake section is the most sophisticated per-guest data collection in the platform. These patterns (per-guest notes, dietary fields, consent checkboxes, edit cutoff enforcement) should serve ALL event types, not just cannabis.

| #   | Question                                                                | Scope       | Verdict     | Evidence                                                                                                                                                                                                             |
| --- | ----------------------------------------------------------------------- | ----------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | Can guests submit cannabis intake forms through public RSVP?            | [CANNABIS]  | **PASS**    | `portal-client.tsx:206-291` initializes all cannabis fields, submits via `saveGuestEventPortalRSVP()`. `sharing/actions.ts:3290-3311` writes every cannabis field to `guest_event_profile`. Full guest self-service. |
| B2  | Does dietary/restriction collection work for non-cannabis events?       | [UNIVERSAL] | **PASS**    | Dietary, accessibility, menu preference fields render independently of `cannabisEnabled`. Cannabis sections gated separately by `showCannabisSections` at line 235. Universal fields always available.               |
| B3  | Is edit cutoff applied to ALL guest edits, not just cannabis?           | [UNIVERSAL] | **PASS**    | Both paths use `parseEditCutoff()`. Chef: `cannabis-actions.ts:541-543`. Guest RSVP: `sharing/actions.ts:3188-3197`. Same function, same enforcement on all fields.                                                  |
| B4  | Is there conflict detection when guest and chef both edit same profile? | [CANNABIS]  | **FAIL**    | No optimistic locking on `guest_event_profile`. No version column. Both paths use blind upsert on `event_id,guest_token`. Last write wins. Contrasts with reconciliation which HAS locking.                          |
| B5  | Does the consent pattern generalize beyond cannabis?                    | [UNIVERSAL] | **PARTIAL** | Consent columns are cannabis-specific (`voluntary_acknowledgment`, `alcohol_acknowledgment`, `transportation_acknowledgment`). Only `final_confirmation` is universal. No extensible consent framework.              |

**Score: 3 PASS, 1 PARTIAL, 1 FAIL = 3.5 / 5**

---

## C. Compliance Document Infrastructure (5 questions)

The control packet system (snapshot versioning, reconciliation, evidence upload, finalization lock, immutable audit trail) is production-grade infrastructure. Most patterns apply to ANY chef needing documentation (health permits, insurance, venue contracts, HACCP records).

| #   | Question                                                                                         | Scope       | Verdict     | Evidence                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------ | ----------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Is cannabis evidence upload the only structured compliance upload, or is there a generic system? | [UNIVERSAL] | **FAIL**    | Multiple upload systems with varying maturity: cannabis has DB evidence table + immutability triggers + RLS + private bucket. Receipts (`lib/expenses/receipt-upload.ts`) have signed URLs but no immutability. Vendor docs (`lib/vendors/document-intake-actions.ts`) have processing pipeline but different security. No consistent pattern. |
| C2  | Does the finalization lock pattern exist for other immutable records?                            | [UNIVERSAL] | **PASS**    | Consistent across: `ledger_entries` (prevent_ledger_mutation, fully append-only), `event_state_transitions` + `quote_state_transitions` (prevent_transition_mutation), event pricing after acceptance, allergy fields after acceptance. All use BEFORE UPDATE/DELETE triggers.                                                                 |
| C3  | Could the blank template be generalized for all events?                                          | [UNIVERSAL] | **PARTIAL** | `generateSeatBlueprint()` in `control-packet-engine.ts:89-121` is pure geometry (zero cannabis refs). But the template page has cannabis labels: "Dose / Skip / Out", "Total Syringes Portioned", "Extract Label Strength". Engine extractable; UI needs redesign.                                                                             |
| C4  | Is `CompliancePlaceholderClient` dead code?                                                      | [CANNABIS]  | **PASS**    | **FIXED:** Dead file `compliance-placeholder-client.tsx` deleted. Zero imports existed. Superseded by control packet system.                                                                                                                                                                                                                   |
| C5  | Does reconciliation have utility beyond cannabis?                                                | [UNIVERSAL] | **PARTIAL** | `evaluateReconciliation()` checks: missing entries, planned vs served mismatch, course count mismatch, invalid sums. Algorithm is generic "planned vs actual." But data model uses `totalMgPlanned`, `totalMgServed`, `breakdownPerCourseMg`. Field names are cannabis-specific, pattern is universal.                                         |

**Score: 2 PASS, 2 PARTIAL, 1 FAIL = 3.0 / 5**

---

## D. Financial Isolation & Ledger Integrity (5 questions)

Cannabis has a separate financial view for compliance/tax reasons (IRC Section 280E). The ledger-first model is platform-wide. Both must be airtight.

| #   | Question                                                                                                                                                                                          | Scope       | Verdict     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Does the cannabis ledger compute totals using the exact same formula as the main event financial summary view, or could they diverge?                                                             | [UNIVERSAL] | **FAIL**    | Cannabis uses `getCannabisLedger()` with manual `filter + reduce` over raw entries. Main financials use `event_financial_summary` DB view. Two different calculation paths: cannabis ledger includes tips in revenue, main view excludes them. Cannabis ledger pulls expenses from `ledger_entries`, main view pulls from `event_expenses` table. Different sources, different formulas, guaranteed divergence on events with tips or expenses in both tables. |
| D2  | If a cannabis event has both cannabis and non-cannabis line items (e.g., food cost + cannabis supplies), are they correctly counted or double-counted across the main ledger and cannabis ledger? | [CANNABIS]  | **PARTIAL** | `getCannabisLedger()` filters by event IDs where `cannabis_preference = true`, then pulls ALL ledger entries for those events. A $500 food cost on a cannabis event appears in both the main financial summary and the cannabis ledger. This is by design (full event view), but nowhere in the UI does it explain this is an overlay, not a partition. A chef summing both views would double-count.                                                          |
| D3  | Does the `cannabis` tax class in the commerce engine have any tax rate configured, or is it a placeholder enum value with no business logic?                                                      | [CANNABIS]  | **FAIL**    | Tax class enum includes `'cannabis'` in the commerce engine migration. No tax calculation function references this class. No rate configured. No code path reads this enum value. Pure placeholder with zero business logic attached.                                                                                                                                                                                                                          |
| D4  | Are cannabis-related refunds correctly reflected in both the cannabis ledger AND the main financial summary? Do the numbers agree?                                                                | [UNIVERSAL] | **PASS**    | **FIXED:** `getCannabisLedger()` now uses `Math.abs()` on refund `amount_cents` (line 246), matching the DB view's `ABS()` pattern. Refunds stored as negative, both views now treat them as positive for subtraction. Numbers agree.                                                                                                                                                                                                                          |
| D5  | Does the platform surface the IRC Section 280E implications anywhere? The cannabis ledger exists for tax separation, but does the chef know WHY?                                                  | [CANNABIS]  | **FAIL**    | No UI element, tooltip, info panel, or documentation explains 280E. The cannabis ledger page shows financial data without context. A chef using the ledger for tax filing has no idea it exists for federal deduction limitation compliance. The structural separation is invisible to the user it's meant to serve.                                                                                                                                           |

**Score: 1 PASS, 1 PARTIAL, 3 FAIL = 1.5 / 5**

---

## E. Email Intelligence Pipeline (5 questions)

Cannabis keyword detection is baked into the email intelligence pipeline. These patterns serve ALL inquiry types.

| #   | Question                                                                                                                                                     | Scope       | Verdict     | Evidence                                                                                                                                                                                                                                                                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| E1  | Does `extractCannabisMentions()` have false positive risk? Could "edible" in a non-cannabis food context trigger cannabis classification?                    | [UNIVERSAL] | **PASS**    | **FIXED:** Singular "edible" now only matches when a strong cannabis signal (cannabis, THC, marijuana, infused meal) is already present. Plural "edibles" still matches standalone (almost always cannabis context). "Edible flowers", "edible gold leaf" no longer false-positive.                                      |
| E2  | Is the `has_cannabis` lead scoring signal (weight: 0, -8.7% lift) actively degrading predictions, or is zero weight truly neutral?                           | [UNIVERSAL] | **PASS**    | Weight 0 is mathematically neutral: contributes nothing to the score. The -8.7% lift statistic is informational only (used during model tuning). The feature occupies memory but causes no prediction error. Removing it would be cleaner but not a bug.                                                                 |
| E3  | When an email thread has cannabis mentions extracted, are those mentions visible to the chef in the inquiry detail view?                                     | [UNIVERSAL] | **PASS**    | `inquiries/[id]/page.tsx:767-771` renders `confirmed_cannabis_preference` in the inquiry detail. The extraction result is visible to the chef. Not hidden.                                                                                                                                                               |
| E4  | If a client emails about a non-cannabis event but casually mentions "420" as a date (April 20th), does the system incorrectly flag it as a cannabis inquiry? | [UNIVERSAL] | **PASS**    | **FIXED:** "420" now excludes false positives: `$420` (dollar amounts), `#420` (addresses), and `420 guests/people/Main St/etc.` (context words). Standalone "420" in cannabis context still matches. Date format "4/20" was never matched (slash breaks word boundary).                                                 |
| E5  | Does the goldmine audit script (`scripts/goldmine-audit.mjs`) have test cases for cannabis extraction false positives, not just true positives?              | [UNIVERSAL] | **PARTIAL** | Test suite at lines 457-498 validates true positives (cannabis keywords detected correctly). Lines 710-734 have some negative cases but they test for absence of OTHER extractors, not false-positive cannabis matches on food-industry terms like "edible flowers" or date-context "420". Weak false-positive coverage. |

**Score: 4 PASS, 1 PARTIAL, 0 FAIL = 4.5 / 5**

---

## F. Access Control & Module Architecture (5 questions)

Cannabis uses an invitation-only tier system. The module/feature-gate pattern should be consistent across the platform.

| #   | Question                                                                                                                                                                                                      | Scope       | Verdict     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | Is the `module: 'cannabis'` gating in nav config consistent with how other modules are gated? Or does cannabis use a bespoke mechanism?                                                                       | [UNIVERSAL] | **FAIL**    | Cannabis uses `module: 'cannabis'` in `chef-nav-config.ts` but items array is empty (disabled). Cannabis is NOT in any archetype's `enabledModules`. Instead, it's gated via `cannabis_tier_users` table lookup in `hasCannabisAccess()`. Other modules use archetype-based `enabledModules`. Cannabis is a completely bespoke gating mechanism, inconsistent with every other module.                                                                                              |
| F2  | When the admin sidebar link for cannabis is commented out (`components/admin/admin-sidebar.tsx` line 49) but the admin nav config includes it (`admin-nav-config.ts` line 90), which one controls visibility? | [CANNABIS]  | **PARTIAL** | The admin sidebar component (`admin-sidebar.tsx`) has the link commented out. But `admin-nav-config.ts` includes cannabis in the nav array. The sidebar renders from the component, not the config. Two sources of truth, the component wins at runtime, but the config misleads developers reading the nav architecture. Dead config entry.                                                                                                                                        |
| F3  | Does `hasCannabisAccess()` correctly handle the case where a user has cannabis tier access but their subscription has lapsed (if cannabis is `tier: 'paid'` in feature classification)? Which gate wins?      | [CANNABIS]  | **FAIL**    | `cannabis-portal` is `tier: 'paid'` in `feature-classification.ts`. Cannabis layout calls `hasCannabisAccess()` which checks `cannabis_tier_users` only. It does NOT check subscription status. `requirePro()` is not called in the cannabis layout. A chef with cannabis tier access but expired subscription bypasses the billing gate entirely. The two gating systems are unaware of each other.                                                                                |
| F4  | Is the cannabis invitation referral chain auditable? Can admin trace who invited whom in a complete graph from original admin grant through N levels of referral?                                             | [CANNABIS]  | **PARTIAL** | `cannabis_tier_invitations` has `invited_by_auth_user_id` linking invite to inviter. But `cannabis_tier_users` (created when invite is claimed) stores `granted_by_admin_email` and `auth_user_id` without referencing the `invitation_id` that spawned the grant. Admin can see invites and users separately. The link between "this user claimed that invite" exists only in the `notes` field as free text, not as a foreign key. Chain is traceable manually but not queryable. |
| F5  | Does the platform have a generic "feature module" system that cannabis, operations, marketing, etc. all plug into, or does each vertical reinvent its own gating mechanism?                                   | [UNIVERSAL] | **FAIL**    | Four independent gating mechanisms: (1) `cannabis_tier_users` table for cannabis, (2) `requirePro()` / `feature-classification.ts` for billing, (3) `requireAdmin()` for admin features, (4) archetype `enabledModules` for module toggles. Each was built independently. Adding a new vertical requires choosing which mechanism(s) to use and potentially combining them. No unified module registry. No single "is feature X enabled for user Y" function.                       |

**Score: 0 PASS, 2 PARTIAL, 3 FAIL = 1.0 / 5**

---

## G. Disabled Feature Integrity (5 questions)

Two pages are hard-disabled (public claim, public cannabis). The nav is empty. Multiple systems reference cannabis features that are "off." Code exists but is unreachable, which can rot silently.

| #   | Question                                                                                                                                                                                                        | Scope      | Verdict     | Evidence                                                                                                                                                                                                                                                                                                                                                                        |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1  | With the public claim page disabled (`/cannabis-invite/[token]` redirects to `/`), what happens when admin approves an invite and the system sends an email with a claim URL? The recipient clicks a dead link. | [CANNABIS] | **FAIL**    | `approveInvite()` in `cannabis-actions.ts:209` constructs `claimUrl = ${appUrl}/cannabis-invite/${token}` and sends `CannabisInviteApprovedEmail`. The public claim page redirects to `/`. Admin approval triggers a professional email delivering a dead link. Actively broken: the system sends emails it knows lead nowhere.                                                 |
| G2  | Is the `cannabisSectionItems = []` empty array in `chef-nav-config.ts` the correct disable mechanism, or should the layout access check be the only gate?                                                       | [CANNABIS] | **PARTIAL** | A chef WITH cannabis tier access can navigate directly to `/cannabis` and see full content (layout checks `hasCannabisAccess()`, passes). But nav shows nothing because items array is empty. Feature is reachable but undiscoverable. This is effectively stealth mode. Unclear if intentional soft-launch or oversight.                                                       |
| G3  | Are there `revalidatePath('/cannabis')` calls firing for a feature that has no active nav? Do they waste compute?                                                                                               | [CANNABIS] | **PARTIAL** | `cannabis-host-agreement-actions.ts:98-101` revalidates 4 cannabis paths on every agreement sign. Next.js `revalidatePath` on uncached paths is a no-op at the cache layer but still executes the function call and path resolution. Minimal waste but indicates the code assumes the feature is active.                                                                        |
| G4  | Does the disabled `/api/cannabis/rsvps/[eventId]/summary` endpoint (returns 404) appear in any API documentation or client code?                                                                                | [CANNABIS] | **FAIL**    | The RSVP summary export endpoint exists in the file tree but returns 404. The cannabis RSVP dashboard (`app/(chef)/cannabis/page.tsx`) has an "Export" button that calls this endpoint. Clicking export on an active cannabis event hits a 404. Silent failure on a user-initiated action.                                                                                      |
| G5  | What is the minimum change set to fully enable the cannabis portal? Is it a single toggle or a multi-file operation?                                                                                            | [CANNABIS] | **PARTIAL** | Minimum 4 files: (1) restore nav items in `chef-nav-config.ts`, (2) un-disable public claim page route, (3) un-disable public cannabis info page, (4) uncomment admin sidebar link in `admin-sidebar.tsx`. No single toggle. No `CANNABIS_ENABLED` env var. Each file must be edited independently, creating risk of partial enablement where some paths work and others don't. |

**Score: 0 PASS, 3 PARTIAL, 2 FAIL = 1.5 / 5**

---

## H. Data Security & Privacy (5 questions)

Cannabis guest data (consumption preferences, familiarity, dosing) is sensitive health/behavioral information. Security posture must match sensitivity. Most of these patterns affect ALL user data, not just cannabis.

| #   | Question                                                                                                                                                                                                     | Scope       | Verdict     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H1  | With RLS globally disabled (`20260401000098_disable_rls_all_tables.sql`), can any authenticated user query another chef's cannabis guest profiles via the Supabase client?                                   | [UNIVERSAL] | **PARTIAL** | RLS is globally disabled. All DB queries go through server actions via the compat layer, which enforces tenant scoping in application code. No client-side Supabase queries found for cannabis tables. Defense-in-depth is missing (no DB-level protection), but the application layer holds. Cannabis data (consumption habits, dosing) has higher privacy sensitivity than other tenant data, making the missing RLS layer more consequential here.                                                            |
| H2  | Are cannabis guest intake fields (familiarity level, dosing preferences, comfort notes) included in data export? Does a chef exporting their data get guests' cannabis consumption preferences?              | [UNIVERSAL] | **FAIL**    | Data export (`app/(chef)/settings/data-export/`) does not include `guest_event_profile` cannabis fields. But the failure is the opposite: cannabis intake data is absent from the export entirely. A chef who needs their own compliance records (guest consent, familiarity levels, dosing preferences) cannot export them. Regulatory-facing data with no export path.                                                                                                                                         |
| H3  | Does the allergy card generator (`lib/documents/generate-allergy-card.ts`) leak cannabis intake data into printed materials?                                                                                 | [UNIVERSAL] | **PASS**    | Allergy card generator reads only dietary restrictions, allergies, and accessibility fields from `guest_event_profile`. Cannabis-specific columns (`familiarity_level`, `dosing_preference`, `comfort_notes`, `consumption_method`) are not referenced. No leak into printed materials.                                                                                                                                                                                                                          |
| H4  | When a chef's account is deleted, are all cannabis DB records cascade-deleted? Does storage cleanup correctly purge the `cannabis-control-packets` bucket?                                                   | [CANNABIS]  | **PARTIAL** | `lib/compliance/storage-cleanup.ts:18` includes `cannabis-control-packets` bucket in cleanup list. Storage is handled. But cannabis tables use `tenant_id` / `chef_id` FK references to `chefs(id)`. Some have `ON DELETE CASCADE`, others use `ON DELETE SET NULL` or no cascade. `cannabis_host_agreements` and `cannabis_control_packets` cascade. `cannabis_tier_users` references `auth_user_id` (not `chefs.id`), so account deletion via chefs table doesn't cascade to tier access. Partial orphan risk. |
| H5  | Is the evidence photo upload validated for content type at both the application layer AND the storage bucket level? Could someone upload a non-image file (e.g., executable) to the control packet evidence? | [CANNABIS]  | **PASS**    | `cannabis-control-packet-actions.ts` evidence upload validates MIME type at application layer (accepts `image/*` only, rejects other types before storage). Storage layer uses signed URL with content-type header enforcement (MIME must match). Double validation present. Cannot upload executables or non-image files.                                                                                                                                                                                       |

**Score: 2 PASS, 2 PARTIAL, 1 FAIL = 3.0 / 5**

---

## I. Cross-Vertical Pattern Extraction (5 questions)

The biggest leverage: infrastructure built for cannabis that should serve everyone. These questions identify where cannabis-specific code could become platform-wide capability.

| #   | Question                                                                                                                                                                                                                                                                        | Scope       | Verdict     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I1  | The host agreement system (text snapshot + SHA-256 hash + immutability trigger + signature + timestamp + IP) is general-purpose legal document signing infrastructure. Does the existing contract system (`lib/contracts/`) use the same immutability pattern, or is it weaker? | [UNIVERSAL] | **FAIL**    | Contracts (`lib/contracts/`) store generated text and a `signed_at` timestamp. No SHA-256 hash of content. No immutability trigger. No IP logging. No hash recomputation on read to verify integrity. The cannabis host agreement system is strictly more robust: hash verification, `prevent_agreement_mutation` trigger, IP capture, signature field. The platform's best legal signing infrastructure is locked behind a cannabis-only gate.                                                                                                                     |
| I2  | The control packet "snapshot -> reconcile -> evidence -> finalize" pattern is a general "plan -> execute -> verify -> lock" workflow. What other ChefFlow features follow this pattern but implement it less rigorously?                                                        | [UNIVERSAL] | **PARTIAL** | Events follow "plan (menu) -> execute (in_progress) -> complete" but have no reconciliation step (planned vs. served comparison), no evidence upload, and no finalization lock on the event record. Quotes have state transitions with immutability triggers (closer to control packets). Contracts have draft -> signed but no reconciliation or evidence. Only control packets have the full 4-step pattern with DB-trigger enforcement at every stage.                                                                                                           |
| I3  | The RSVP dashboard's per-guest editing with Zod validation, edit cutoff enforcement, and Supabase storage is more sophisticated than other per-entity editors. Could this pattern be extracted into a reusable "managed entity editor"?                                         | [UNIVERSAL] | **PARTIAL** | Client editing (`lib/clients/actions.ts`) has Zod validation but no edit cutoff. Recipe editing (`lib/recipes/actions.ts`) has Zod validation but no edit window enforcement. Staff management has basic validation. None enforce time-based edit windows. The cannabis guest editor is the only entity editor with: Zod + edit cutoff + per-field granular updates + optimistic conflict warning. Pattern is extractable but currently cannabis-only.                                                                                                              |
| I4  | The invitation system (send -> admin approve -> token -> email -> claim -> grant) is a complete referral pipeline. How many separate invitation systems exist in ChefFlow, and could they share infrastructure?                                                                 | [UNIVERSAL] | **FAIL**    | 5+ independent invitation systems identified: (1) cannabis tier invitations (`cannabis_tier_invitations` table, full admin approval flow), (2) collaboration invitations (`lib/collaboration/`), (3) client portal invitations (share token generation), (4) staff invitations (`lib/staff/`), (5) network/partner invitations. Each is bespoke. Zero shared code. Each reimplements token generation, expiry, email sending, and claim logic independently. Cannabis has the most complete implementation (admin approval, audit trail, revocation, regeneration). |
| I5  | The audit logging added for cannabis (`logCannabisAudit()`) fills a gap across the entire platform. Are there mutation-heavy areas (financial operations, client data changes, recipe modifications) that lack any audit logging?                                               | [UNIVERSAL] | **FAIL**    | `logAdminAction()` covers admin operations. `logCannabisAudit()` covers cannabis chef operations. Nothing covers: event creation/updates/deletion, client creation/updates, recipe modifications, quote state changes, ledger entry creation, menu changes, staff changes, settings changes. The entire chef-level mutation surface (the core product) has zero audit logging. Cannabis exposed a platform-wide gap: only admin and cannabis actions are audited, everything else is fire-and-forget.                                                               |

**Score: 0 PASS, 2 PARTIAL, 3 FAIL = 1.0 / 5**

---

## J. End-to-End Flow Verification (5 questions)

Complete user journeys across system boundaries. A flow that works in isolation but breaks at a handoff is worse than one that's obviously broken.

| #   | Question                                                                                                                                                                                                                                                                                                                                                      | Scope       | Verdict     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| J1  | **Full cannabis event lifecycle:** Can a chef create a cannabis event, attach a menu, have guests RSVP with cannabis intake data, generate a control packet snapshot, reconcile planned vs. served doses, upload evidence, finalize the packet, and view the event in the cannabis ledger? Does every step connect to the next without manual URL navigation? | [CANNABIS]  | **PARTIAL** | Each individual step works in isolation. But forward navigation is missing between steps: the event page doesn't link to "Generate Control Packet", the control packet page doesn't link to "View in Cannabis Ledger". Chef must know the URL structure or use nav (which is currently empty). Steps work; discoverability fails.                                                                                                                          |
| J2  | **Full invitation lifecycle:** Can a cannabis chef send an invite, have admin approve it, have the system email the claim URL, have the invitee click the link and claim access, sign the host agreement, and then access the cannabis portal? Is every step connected?                                                                                       | [CANNABIS]  | **FAIL**    | Broken at step 4: public claim page (`/cannabis-invite/[token]`) redirects to `/`. The email sent at step 3 delivers a dead link. Even if the claim page were enabled, the flow from claim -> host agreement -> portal has no verified navigation chain. The invitation lifecycle is structurally complete in the DB but broken in the UI.                                                                                                                 |
| J3  | **Non-cannabis event with detailed guest intake:** Can a non-cannabis chef create a regular event, have guests submit dietary restrictions, accessibility notes, and per-guest preferences through the public RSVP portal, and view that intake data in a dashboard? Does the intake system work without cannabis?                                            | [UNIVERSAL] | **PASS**    | Guest RSVP portal renders dietary, accessibility, and preference fields independently of `cannabisEnabled`. Cannabis sections are gated by `showCannabisSections` (line 235 of portal-client.tsx). Non-cannabis events get full guest intake without cannabis fields. The intake system works universally. This is the highest-leverage question and it passes.                                                                                            |
| J4  | **Cross-ledger consistency:** For a cannabis event with payments, does the same financial data appear consistently in: (1) main event financial summary, (2) cannabis ledger, (3) dashboard financial widgets, (4) client-facing payment history? Do the numbers agree across all 4 views?                                                                    | [UNIVERSAL] | **PARTIAL** | Per D1/D4 findings: cannabis ledger and main financial summary use different formulas (tip inclusion, expense source, refund ABS). Dashboard widgets read from cached summaries (third calculation path). Client payment history reads from `ledger_entries` directly (fourth path). Four views, at least three different calculation methods. Numbers will diverge on any event with tips, expenses in both tables, or refunds stored as negative values. |
| J5  | **Compliance documentation export:** If a chef needs to show compliance documentation to a regulator, venue owner, or insurance provider, can they export/print the control packet with all evidence photos, reconciliation data, guest consent records, and the signed host agreement as a single document?                                                  | [CANNABIS]  | **FAIL**    | No PDF export or print-friendly composite view exists. The blank seating template is printable (`generateSeatBlueprint()`). Completed packets with reconciliation data, evidence photos, and consent records have no export path. A chef standing in front of a regulator cannot produce a single document from the system built for regulatory compliance.                                                                                                |

**Score: 1 PASS, 2 PARTIAL, 2 FAIL = 2.0 / 5**

---

## Score Summary

| Domain                          | Questions | Universal | Cannabis | Pre-Fix             | Post-Fix            |
| ------------------------------- | --------- | --------- | -------- | ------------------- | ------------------- |
| A. Inquiry-to-Event Pipeline    | 5         | 4         | 1        | 3.5/5               | 4.5/5               |
| B. Guest Intake & Consent       | 5         | 3         | 2        | 3.5/5               | 3.5/5               |
| C. Compliance Document Infra    | 5         | 4         | 1        | 2.0/5               | 3.0/5               |
| D. Financial Isolation & Ledger | 5         | 2         | 3        | 0.5/5               | 1.5/5               |
| E. Email Intelligence Pipeline  | 5         | 5         | 0        | 2.5/5               | 4.5/5               |
| F. Access Control & Modules     | 5         | 2         | 3        | 1.0/5               | 1.0/5               |
| G. Disabled Feature Integrity   | 5         | 0         | 5        | 1.5/5               | 1.5/5               |
| H. Data Security & Privacy      | 5         | 3         | 2        | 3.0/5               | 3.0/5               |
| I. Cross-Vertical Extraction    | 5         | 5         | 0        | 1.0/5               | 1.0/5               |
| J. End-to-End Flow Verification | 5         | 3         | 2        | 2.0/5               | 2.0/5               |
| **TOTAL**                       | **50**    | **31**    | **19**   | **20.5/50 (41.0%)** | **25.5/50 (51.0%)** |

**31 of 50 questions (62%) are [UNIVERSAL] and benefit all users regardless of cannabis feature status.**

---

## How to Use This Question Set

1. **Investigate each question** by reading the referenced code paths. Mark PASS/PARTIAL/FAIL with cited evidence.
2. **[UNIVERSAL] questions take priority** since fixes benefit all users. 31 of 50 questions are universal.
3. **[CANNABIS] questions** (19 of 50) fix only if cannabis is being re-enabled.
4. **Cross-reference** with:
   - `system-integrity-question-set-cannabis-portal.md` (30.5/40) for portal-internal integrity
   - `system-integrity-question-set-cross-system-cohesion.md` (44/52) for non-cannabis cross-system chains
5. **Target score:** 40/50 (80%) for re-enablement. 45/50 (90%) for production.

---

## The Cohesion Thesis

The cannabis module is ChefFlow's most sophisticated vertical. It was built from a decade of real cannabis dining experience and forced the creation of infrastructure that every chef needs:

| Cannabis Built It                                         | Every Chef Needs It                                                      |
| --------------------------------------------------------- | ------------------------------------------------------------------------ |
| Per-guest intake with consent checkboxes                  | Dietary collection, accessibility notes, allergy acknowledgment          |
| Host agreement with SHA-256 hash + immutability trigger   | Contract signing, liability waivers, venue agreements                    |
| Control packet: snapshot -> reconcile -> evidence -> lock | Event execution verification, post-event review                          |
| Audit trail for all mutations                             | Compliance for catering licenses, health inspections                     |
| Financial category isolation (separate ledger)            | Multi-service-type reporting (private dining vs. meal prep vs. catering) |
| Edit cutoff enforcement (no changes after event time)     | Preventing post-event modifications to any event data                    |
| Invitation referral pipeline (send -> approve -> claim)   | Platform growth, chef network, client referrals                          |
| Evidence photo upload with signed URLs                    | Receipt documentation, venue photos, equipment condition                 |

**The highest-leverage work is not fixing cannabis bugs. It's extracting cannabis infrastructure into platform-wide capability.** Every fix to a [UNIVERSAL] question improves the product for all users, even those who never touch cannabis.

---

## MemPalace Source Index

These insights were mined from 20+ indexed conversations. Key source files:

| Source                          | Content                                                                                       |
| ------------------------------- | --------------------------------------------------------------------------------------------- |
| `agent-a20242664695bd051.jsonl` | Task force deep dive, regulatory framework, About page                                        |
| `agent-a8d3ab45229a4e687.jsonl` | Full cannabis feature audit: 200+ references, all DB tables, Phase 1/2                        |
| `agent-adb8673.jsonl`           | David's personal history: Euphoric Food, Le Cordon Bleu, media, dispensary relationships      |
| `agent-ab6e8e4.jsonl`           | Cannabis event data model debate (boolean vs. structured)                                     |
| `agent-a1e33fa7ddd9cce76.jsonl` | Security vulnerability: guest cannabis preferences exposed via rebook links                   |
| `agent-af5c5d6d4cc6215e0.jsonl` | Legal landscape analysis, gray zone assessment                                                |
| `agent-a403bd7985f46a09c.jsonl` | Guest RSVP form with full cannabis survey fields                                              |
| `agent-a44ecf50482f2f473.jsonl` | Feature list: compliance, control packets, dosage calculation                                 |
| `scripts/parsed-emails.json`    | Real client emails: dosing workflows, pricing, dispensary guidance, consumption questionnaire |

**Developer credentials in this domain:**

- 10+ years producing cannabis dining events
- Maine Cannabis Hospitality Task Force member (1 of 2 culinary arts experts, 15-member panel)
- Co-founder of Euphoric Food (cannabis-elevated cuisine)
- Partnered with James Beard Award winner Chef Jody Adams for CBD dinner
- Media: WBUR, Boston Magazine, Mainebiz, 420 Foodie Club
- Le Cordon Bleu trained; Tuscan Kitchen, Stages, Toro experience

The cannabis portal isn't a speculative feature. It's built from direct operational experience by someone who helped write the regulatory recommendations.
