# Event Ops Kit (Canonical)

Canonical operations toolkit for website-wide service workflows:

- Kit root: `docs/event-ops-kit`
- Primary commands: `npm run ops:*`

Start here:

- `docs/event-ops-kit/README.md`
- `docs/event-ops-kit/BUILD_TRACKER.md`

Legacy compatibility:

- Legacy vertical kit remains at `docs/grazing-table-ops-kit`
- Legacy commands remain available as `npm run grazing:*`
