# ORCHESTRATION MISSION: Rail Intensification (Deep Pass #2)

> Generated by `/intensify rail --deep` + `/swarm-handoff` on 2026-05-16.
> Source: `docs/UNIFIED-BUILD-QUEUE.md` section "RAIL INTENSIFICATION (6 items)"

## Context Load (Read These First)

- `CLAUDE.md` (auto-loaded)
- `docs/specs/dashboard-rail-architecture.md` (the rail system spec)
- `docs/specs/rail-item-lifecycle-and-scoring-engine.md` (scoring + state machine spec)
- `docs/intensify/rail.md` (prior findings, what's been done)
- `lib/discovery/resolvers/chef/intelligence-resolver.ts` (PATTERN: reference resolver to copy structure from)
- `lib/discovery/god-mode-dispatcher.ts` (where resolvers register)
- `lib/discovery/rail-tier-assigner.ts` (assembleTieredRail, tier assignment logic)

## Session Decisions (Do Not Re-Debate)

- Scoring engine migration is DONE. `assembleTieredRail` is the canonical path. Do not touch `assembleGodModeRail`.
- Tier thresholds are 80/50/20. Fixed. Do not adjust.
- The 4-tier system (Critical/Action/Awareness/Opportunity) is final. No 5th tier.
- GodModeRailSection on dashboard page is dead code. Safe to remove entirely.
- `rail-full.tsx` and `rail-tier-group.tsx` are superseded by `tiered-rail.tsx` + `tier-row.tsx`.
- All new resolvers follow the pattern in `intelligence-resolver.ts`: export async function, return `GodModeResolvedItem[]`, register in `god-mode-dispatcher.ts` warmResolvers array.

## Wave 1 (Parallel - Launch All 5 Immediately)

### Agent 1: Lifecycle Journey Resolver

- **Model:** opus
- **Task:** Create `lib/discovery/resolvers/chef/lifecycle-stage-resolver.ts`. Read current journey state via `getJourneyState()` from `lib/lifecycle/journey-orchestrator.ts` and pending triggers via `evaluateTriggers()` from `lib/lifecycle/trigger-engine.ts`. For each active event with a lifecycle stage, emit a rail item. Tier assignment: stage transitions requiring chef action = p1 (Action), informational stage updates = p2 (Awareness), upcoming trigger about to fire = p0 (Critical) if within 24h. Register in `god-mode-dispatcher.ts` warmResolvers.
- **Read first:**
  - `lib/discovery/resolvers/chef/intelligence-resolver.ts` (pattern)
  - `lib/lifecycle/journey-orchestrator.ts` (getJourneyState API)
  - `lib/lifecycle/trigger-engine.ts` (evaluateTriggers API, TRIGGER_RULES)
  - `lib/discovery/god-mode-dispatcher.ts` (registration)
  - `lib/discovery/god-mode-types.ts` (GodModeResolvedItem type)
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Resolver registered. Items appear in tiered rail output when lifecycle events exist.

### Agent 2: Cadence Due Resolver

- **Model:** opus
- **Task:** Create `lib/discovery/resolvers/chef/cadence-due-resolver.ts`. Query upcoming cadence items using the pattern from `lib/communication/cadence-scheduler.ts` (processDueCadenceItems logic). Surface items firing within 48h. Tier: firing within 4h = p0 (Critical), within 24h = p1 (Action), within 48h = p2 (Awareness). Chef gets preview of what auto-sends are coming. Register in `god-mode-dispatcher.ts`.
- **Read first:**
  - `lib/discovery/resolvers/chef/intelligence-resolver.ts` (pattern)
  - `lib/communication/cadence-scheduler.ts` (processDueCadenceItems, cadence points, scheduled_messages query)
  - `lib/discovery/god-mode-dispatcher.ts` (registration)
  - `lib/discovery/god-mode-types.ts` (GodModeResolvedItem type)
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Resolver registered. Upcoming cadence emails visible on rail.

### Agent 3: Dead Code Removal

- **Model:** haiku
- **Task:** Remove dead rail code:
  1. In `app/(chef)/dashboard/page.tsx`: remove `GodModeRailSection` function (line ~2361) and its import of `getGodModeRail` (line 61). Verify it's not referenced in the JSX return.
  2. Delete `components/rail/rail-full.tsx`
  3. Delete `components/rail/rail-tier-group.tsx`
  4. In `lib/discovery/god-mode-assembly.ts`: remove `assembleGodModeRail` export (keep other exports if any exist)
  5. In `lib/discovery/universal-rail-actions.ts`: remove `getGodModeRail` server action export
  6. Clean any broken imports that result
- **Read first:**
  - `app/(chef)/dashboard/page.tsx` (find GodModeRailSection, confirm not in JSX)
  - `components/rail/rail-full.tsx` (confirm no other importers via grep)
  - `components/rail/rail-tier-group.tsx` (confirm no other importers)
  - `lib/discovery/god-mode-assembly.ts` (find assembleGodModeRail)
  - `lib/discovery/universal-rail-actions.ts` (find getGodModeRail)
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. No references to deleted symbols. `tiered-rail.tsx` still renders correctly.

### Agent 4: Completion Resolver

- **Model:** opus
- **Task:** Create `lib/discovery/resolvers/chef/completion-resolver.ts`. Hydrate the 6 declared entries in `lib/discovery/registries/chef-rail-registry.ts` (chef.completion_event, chef.completion_menu, chef.completion_recipe, chef.completion_recursive, chef.completion_client_profile, chef.completion_overall). Use `lib/completion/engine.ts` to call `getCompletionResult()` for active events. Emit rail items for incomplete dependencies. Tier: overall < 50% = p1 (Action), 50-80% = p2 (Awareness), > 80% = p3 (Opportunity, "almost done!"). Register in god-mode-dispatcher.
- **Read first:**
  - `lib/discovery/resolvers/chef/intelligence-resolver.ts` (pattern)
  - `lib/completion/engine.ts` (getCompletionResult API)
  - `lib/completion/types.ts` (CompletionResult shape)
  - `lib/discovery/registries/chef-rail-registry.ts` (lines 2723-2870, the 6 completion entries)
  - `lib/discovery/god-mode-dispatcher.ts` (registration)
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Completion items hydrated when events have incomplete deps.

### Agent 5: Density Caps

- **Model:** haiku
- **Task:** In `lib/discovery/rail-tier-assigner.ts`, add density cap enforcement after tier assignment. Spec caps: Critical = max 3, Action = max 8, Awareness = max 12, Opportunity = max 6. If a tier exceeds cap, keep highest-scored items, drop rest. Simple `.slice(0, cap)` after sorting by score desc within each tier.
- **Read first:**
  - `lib/discovery/rail-tier-assigner.ts` (assembleTieredRail function, find where items are grouped by tier)
  - `docs/specs/dashboard-rail-architecture.md` (density caps section)
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. No tier can exceed its cap regardless of resolver output volume.

## Wave 2 (After Wave 1 Verified)

### Agent 6: Rail Item State Table

- **Model:** opus
- **Task:** Create migration for `rail_item_state` table. Schema from spec (`docs/specs/rail-item-lifecycle-and-scoring-engine.md`): columns for userId, tenantId, itemId, state (enum: surfaced/seen/acted/resolved/expired/archived), seenAt, actedAt, snoozedUntil, dismissedAt. Create thin persistence layer at `lib/discovery/rail-item-state.ts` with: `markSeen(userId, itemId)`, `markActed(userId, itemId)`, `snooze(userId, itemId, until)`, `dismiss(userId, itemId)`, `getStates(userId, itemIds[])`. Do NOT wire into scoring yet (that's a future move). Just build the table + CRUD.
- **Read first:**
  - `docs/specs/rail-item-lifecycle-and-scoring-engine.md` (state machine section)
  - `database/migrations/` (glob for latest timestamp, use higher one)
  - `database/schema.ts` or equivalent (Drizzle schema patterns)
  - `lib/discovery/rail-tier-assigner.ts` (understand item shape for itemId format)
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Migration file exists. State functions exported. Table NOT yet applied (migration only, no push).

## Verification Protocol

- Each agent runs type check: `npx tsc --noEmit --skipLibCheck`
- After Wave 1: orchestrator verifies all 5 agents pass type check collectively (no cross-agent conflicts)
- After Wave 2: full type check + verify migration SQL is valid
- Anti-Loop: 3 strikes on same error = stop, report, let developer decide
- Do NOT run `drizzle-kit push` (migration only, developer applies manually)

## Orchestrator Rules

1. You are the COORDINATOR. You do not write implementation code.
2. Dispatch agents via the Agent tool with appropriate model tier.
3. After dispatching Wave 1 (all 5 agents in parallel), wait for completion.
4. Verify each agent's output (type check per agent, then collective type check).
5. Only proceed to Wave 2 after Wave 1 fully verified.
6. If an agent fails: diagnose, give one retry with better context, then flag.
7. At completion: update `docs/UNIFIED-BUILD-QUEUE.md` RAIL INTENSIFICATION items to DONE/PARTIAL.
8. Update `docs/intensify/rail.md` ACTED ON section with which moves were completed.
9. Commit all work with message: `feat(rail): intensify deep pass #2 - lifecycle/cadence/completion resolvers + dead code cleanup`
