# ORCHESTRATION MISSION: Rail Deep-Pass #3 (Closed-Loop Forge)

> Generated by `/deep-pass rail` on 2026-05-16.
> Source: Phase 4 forge from expert-validated intensification moves.
> Prior art: `docs/handoffs/2026-05-16-rail-intensification-swarm.md` (deep-pass #2, still valid, execute first)

## Context Load (Read These First)

- `CLAUDE.md` (auto-loaded)
- `docs/specs/dashboard-rail-architecture.md` (rail system spec)
- `docs/specs/rail-item-lifecycle-and-scoring-engine.md` (scoring + state machine)
- `docs/intensify/rail.md` (all prior findings + this run)
- `lib/discovery/resolvers/chef/intelligence-resolver.ts` (PATTERN: resolver structure)
- `lib/discovery/god-mode-dispatcher.ts` (resolver registration)
- `lib/discovery/rail-tier-assigner.ts` (assembleTieredRail, scoring pipeline)
- `lib/discovery/universal-rail-connections.ts` (slot policy, complementary boosts)
- `lib/discovery/universal-rail-scoring.ts` (computeUniversalRailScore)
- `lib/discovery/universal-rail-state.ts` (impression/state persistence)

## Session Decisions (Do Not Re-Debate)

- Slot policy caps: nudge 20%, ambient 30%. Already coded, just needs calling.
- Page-affinity boost must not override critical-tier items. Cap: max 2 page-boosted items can outrank a genuine critical.
- Impression tracking threshold: 1.5s viewport time minimum. Batch every 5s.
- DOP tasks aggregate per-event ("3 overdue"), NOT per-task (no task-list-in-rail).
- Multi-event conflicts require real TIME overlap, not just same-date.
- Readiness resolver uses completion-contract engine, not custom readiness logic.
- Finance: overdue payments only. Unusual expenses REJECTED (insufficient data).
- Constraint collisions REJECTED (surface components via readiness blockers instead).
- Rest day warnings REJECTED (patronizing, erodes trust).
- Revenue concentration REJECTED (consulting advice, not operational).

## Wave 1 (Parallel - Foundation Wiring, 4 agents)

### Agent 1: Slot Policy Enforcement

- **Model:** haiku
- **Task:** In `lib/discovery/rail-tier-assigner.ts`, find where items are grouped into tiers after scoring (inside `assembleTieredRail`). After tier assignment and sorting by score desc, call `applyOperationalSlotPolicy()` from `lib/discovery/universal-rail-connections.ts` on the merged item array BEFORE returning. Import it. The function already handles classification and caps (nudge 20%, ambient 30%). Pass the full item array; it returns the capped array.
- **Read first:**
  - `lib/discovery/rail-tier-assigner.ts` (assembleTieredRail, find post-scoring merge point)
  - `lib/discovery/universal-rail-connections.ts` (applyOperationalSlotPolicy export + signature)
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Function is called in the assembly pipeline.

### Agent 2: Page-Affinity Wiring

- **Model:** haiku
- **Task:** In `lib/discovery/rail-tier-assigner.ts`, find `scoreGodModeItem` (around line 134). It hardcodes `currentPage: null`. Change `assembleTieredRail` to accept an optional `currentPage?: string` parameter. Pass it through to `scoreGodModeItem` which should pass it to `computeUniversalRailScore`. Then in the caller (search for all call sites of `assembleTieredRail`), pass `currentPage: "/dashboard"` or the appropriate page path.
- **Read first:**
  - `lib/discovery/rail-tier-assigner.ts` (scoreGodModeItem, assembleTieredRail signature)
  - `lib/discovery/universal-rail-scoring.ts` (computeUniversalRailScore params)
  - Grep for `assembleTieredRail(` to find all callers
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. `currentPage` flows from caller through assembly to scoring.

### Agent 3: Impression Data Into Scoring

- **Model:** haiku
- **Task:** In `lib/discovery/rail-tier-assigner.ts`, find where `loadRailUserState` is called (around line 296). It returns impression data. Find the scoring loop (around lines 335-339) where `scoreGodModeItem` is called with `impressionCount: 0` and `lastSeenAt: null`. Replace those zeros with actual values from the loaded state's impressions map. Match items by their itemId to look up impression count and last seen timestamp from the state.
- **Read first:**
  - `lib/discovery/rail-tier-assigner.ts` (loadRailUserState call + scoring loop)
  - `lib/discovery/universal-rail-state.ts` (return shape of loaded impressions)
  - `lib/discovery/universal-rail-scoring.ts` (impressionCount + lastSeenAt params)
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Scoring receives real impression data. Items seen 10+ times score lower.

### Agent 4: UI Impression Tracking

- **Model:** opus
- **Task:** In `components/rail/tier-row.tsx` or `components/rail/rail-item-row.tsx` (whichever renders individual items), add IntersectionObserver tracking. Items visible for > 1.5 seconds get batched. Call `trackRailImpression` (server action from `lib/discovery/universal-rail-actions.ts`) in debounced batches every 5 seconds. Use `useEffect` + `useRef` + `startTransition`. Only track at 0.5 intersection threshold. Do NOT fire during SSR.
- **Read first:**
  - `components/rail/tier-row.tsx` (item render)
  - `components/rail/rail-item-row.tsx` (if separate item component exists)
  - `lib/discovery/universal-rail-actions.ts` (trackRailImpression export + signature)
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Network tab shows periodic impression batch calls when viewing rail items for 1.5s+.

## Wave 2 (After Wave 1 Verified - P0 Resolvers, 3 agents)

### Agent 5: DOP Task Digest Resolver

- **Model:** opus
- **Task:** Create `lib/discovery/resolvers/chef/dop-task-digest-resolver.ts`. Import `getDOPTaskDigest` from `lib/scheduling/task-digest.ts`. Call for current user's upcoming events. Map to `GodModeResolvedItem[]`: AGGREGATE per-event. One rail item per event with overdue tasks: title like "Saturday dinner: 3 prep tasks overdue." Tier: overdue = p0 (Critical), due-today = p1 (Action). Register in `god-mode-dispatcher.ts` warmResolvers.
- **Read first:**
  - `lib/discovery/resolvers/chef/intelligence-resolver.ts` (PATTERN)
  - `lib/scheduling/task-digest.ts` (getDOPTaskDigest API + return shape)
  - `lib/discovery/god-mode-dispatcher.ts` (warmResolvers registration)
  - `lib/discovery/god-mode-types.ts` (GodModeResolvedItem type)
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Resolver registered. Overdue DOP tasks surface as aggregated per-event items.

### Agent 6: Multi-Event Conflict Resolver

- **Model:** haiku
- **Task:** Create `lib/discovery/resolvers/chef/multi-event-day-resolver.ts`. Import `getMultiEventDays` from `lib/scheduling/multi-event-days.ts`. Surface days with 2+ events within next 14 days. One rail item per conflict day. Tier: same-day TIME OVERLAP within 7 days = p0 (Critical), same-day NO overlap within 7 days = p1 (Action), 8-14 days out = p2 (Awareness). Register in dispatcher.
- **Read first:**
  - `lib/discovery/resolvers/chef/intelligence-resolver.ts` (PATTERN)
  - `lib/scheduling/multi-event-days.ts` (getMultiEventDays API + return shape)
  - `lib/discovery/god-mode-dispatcher.ts` (registration)
  - `lib/discovery/god-mode-types.ts`
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Multi-event days appear on rail with time-aware tier assignment.

### Agent 7: Event Readiness Resolver

- **Model:** opus
- **Task:** Create `lib/discovery/resolvers/chef/event-readiness-resolver.ts`. For each confirmed/paid event within 7 days, call completion engine (`lib/completion/engine.ts` `getCompletionResult()`). If < 100% and has blockers, emit ONE rail item per event: "Saturday dinner: 3 blockers (menu, deposit, guest count)." Tier: blockers on event within 48h = p0 (Critical), within 7 days = p1 (Action). Register in dispatcher.
- **Read first:**
  - `lib/discovery/resolvers/chef/intelligence-resolver.ts` (PATTERN)
  - `lib/completion/engine.ts` (getCompletionResult API)
  - `lib/completion/types.ts` (CompletionResult shape)
  - `lib/discovery/god-mode-dispatcher.ts` (registration)
  - `lib/discovery/god-mode-types.ts`
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Events within 7 days with blockers surface with aggregated blocker count.

## Wave 3 (After Wave 2 Verified - P1 Resolvers, 2 agents)

### Agent 8: Finance Overdue Resolver

- **Model:** haiku
- **Task:** Create `lib/discovery/resolvers/chef/finance-overdue-resolver.ts`. Import `getFinanceAlerts` from `lib/finance/alert-actions.ts`. If `overdueCount > 0`, emit ONE rail item: "$X overdue across N invoices." Tier: > $1000 = p1 (Action), < $1000 = p2 (Awareness). Do NOT surface unusual expenses. Register in dispatcher.
- **Read first:**
  - `lib/discovery/resolvers/chef/intelligence-resolver.ts` (PATTERN)
  - `lib/finance/alert-actions.ts` (getFinanceAlerts return shape)
  - `lib/discovery/god-mode-dispatcher.ts` (registration)
  - `lib/discovery/god-mode-types.ts`
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Overdue finance alerts surface on rail.

### Agent 9: Stripe Disputes Resolver

- **Model:** opus
- **Task:** Create `lib/discovery/resolvers/chef/dispute-resolver.ts`. Import dispute query from `lib/finance/dispute-actions.ts`. Surface open disputes. Each open dispute = one rail item (disputes are rare, individual attention warranted). Tier: ALWAYS p0 (Critical). Include days remaining until deadline in description. Register in dispatcher. If no disputes table or webhook handler exists, create the resolver structure and add a TODO comment noting webhook dependency.
- **Read first:**
  - `lib/discovery/resolvers/chef/intelligence-resolver.ts` (PATTERN)
  - `lib/finance/dispute-actions.ts` (getDisputes or similar export)
  - `lib/discovery/god-mode-dispatcher.ts` (registration)
  - `lib/discovery/god-mode-types.ts`
  - Grep for "dispute" in `database/schema.ts` or migrations to confirm table exists
- **Done when:** `npx tsc --noEmit --skipLibCheck` passes. Open disputes surface as Critical tier items with deadline info.

## Verification Protocol

- Each agent: `npx tsc --noEmit --skipLibCheck` must pass
- After Wave 1: collective type check (no cross-agent conflicts in rail-tier-assigner.ts)
- After Wave 2: type check + verify resolvers register without collision
- After Wave 3: full type check + `npm run test:experiential`
- Anti-Loop: 3 strikes on same error = stop, report, developer decides

## Orchestrator Rules

1. You are the COORDINATOR. You do not write implementation code.
2. Dispatch agents via Agent tool with appropriate model tier.
3. Wave 1: all 4 agents in parallel. Wait for completion.
4. Verify Wave 1 collectively (slot policy + affinity + impressions all touch rail-tier-assigner.ts - check for merge conflicts).
5. Wave 2: all 3 agents in parallel after Wave 1 verified.
6. Wave 3: both agents in parallel after Wave 2 verified.
7. If agent fails: diagnose, one retry with better context, then flag.
8. At completion: update `docs/intensify/rail.md` BUILD_PROMPTS status to DISPATCHED.
9. Commit: `feat(rail): deep-pass #3 - foundation wiring + P0/P1 resolvers`

## Relationship to Deep-Pass #2

The prior handoff (`2026-05-16-rail-intensification-swarm.md`) covers:

- Lifecycle journey resolver
- Cadence due resolver
- Dead code removal
- Completion resolver (different scope from readiness resolver here)
- Density caps
- Rail item state table

Those are SEPARATE work. Execute #2 first (it's more foundational), then this #3 handoff. OR execute Wave 1 of both in parallel since they touch different files (except rail-tier-assigner.ts which both modify).
