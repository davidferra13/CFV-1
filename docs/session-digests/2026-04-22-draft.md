# Session Digest: Task and Reminder Contract Truth

**Date:** 2026-04-22
**Agent:** Codex
**Branch:** main
**Commits:** pending at digest update time

## What Changed

- Closed the task/reminder contract drift without introducing a second owner.
- Added `lib/todos/match.ts` so reminder completion can stay on `chef_todos` and fail closed on unmatched text.
- Rewired Remy and briefing/proactive task readers to use structured `tasks` with the real `chef_id`, `status`, `due_date`, and `priority` fields.
- Kept lightweight reminder completion on the canonical todo helpers in `lib/todos/actions.ts`.
- Updated prompt assembly, blueprint/map docs, build-state, and the completeness/control-tower docs so this lane no longer reads as open.

## What Was Validated

- Focused typecheck passed with a temporary tsconfig covering the touched task/reminder files.
- `node --test --import tsx tests/unit/task-todo-contract.test.ts` passed.
- The failure path is covered: unmatched reminder text returns `null`, and workflow completion reports `No open reminders match that description.`
- `graphify update .` completed after the slice landed.
- Direct Playwright verification passed on isolated `http://127.0.0.1:3210` using seeded chef auth through `/api/e2e/auth`: `/tasks` rendered the real task board, empty state, and `New Task` modal without route or runtime failure.

## What Remains Open

- The pre-existing `/tasks` create form still did not persist a new DB row during browser verification, even though the server-action posts returned `200`. That appears to be a separate task-board mutation issue, not part of this contract slice.
- Repo-wide typecheck remains blocked by unrelated dirty-checkout errors outside the touched files.

## What The Next Agent Should Do

1. If the next assigned lane touches task-board mutation UX, inspect the existing `/tasks` create flow directly before assuming the route is fully healthy.
2. Otherwise, leave the task/reminder ownership split alone and build on the current owners: `tasks` for structured work, `chef_todos` for lightweight reminders.
3. Resume the higher-priority unsolved lanes, especially source provenance and trust-loop arbitration, unless current repo evidence redirects that choice.
