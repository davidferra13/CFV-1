Read and execute `docs/specs/build-client-relationship-closure-guards-and-tests.md`.

Follow it exactly. Start only after `docs/specs/build-client-relationship-closure-server-actions.md` has landed. You can run in parallel with the UI agent. Run all verification commands allowed by the spec at the end. If verification fails after 2 attempts, git stash your changes and report what failed. Do not fix unrelated issues.
