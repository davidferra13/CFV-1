# UI Orphan Lanes

- Date: 2026-05-01
- Scope: focused duplicate-wrapper proof under `components/ui`
- Owner: `docs-release-proof`
- Rule: delete only when exact import reachability is zero and a canonical replacement owner exists

## Duplicate Wrapper Cleanup

| Candidate                                      | Decision | Replacement owner proof                                                                                                                                                                                                                              | Reachability proof                                                                                                                             |
| ---------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `components/ui/confirm-destructive-dialog.tsx` | Deleted  | Thin wrapper around `components/ui/confirm-modal.tsx`, which owns accessible confirmation dialogs and danger variants.                                                                                                                               | No exact import refs across `app`, `components`, `lib`, `tests`, `scripts`, or `docs`. Symbol refs were historical docs only.                  |
| `components/ui/conflict-dialog.tsx`            | Deleted  | `components/ui/conflict-resolution-dialog.tsx` owns the record conflict flow with attempted/latest comparison and keep-latest/keep-mine actions.                                                                                                     | No exact import refs and no `ConflictDialog` symbol refs outside the candidate.                                                                |
| `components/ui/draft-save-indicator.tsx`       | Deleted  | `components/ui/save-state-badge.tsx` owns visible save state, retry, offline queue, failure, and saved timestamps through `lib/save-state/model`.                                                                                                    | No exact import refs and no `DraftSaveIndicator` symbol refs outside the candidate.                                                            |
| `components/ui/error-boundary.tsx`             | Deleted  | `components/ui/widget-error-boundary.tsx` owns granular client render isolation with retry UI and is the live dashboard/client pattern. Route-level `error.tsx` files own full-page crashes.                                                         | No exact import refs. `ErrorBoundary` refs are docs, tests that crawl rendered pages, or `WidgetErrorBoundary` usage.                          |
| `components/ui/shortcuts-help-panel.tsx`       | Deleted  | `components/keyboard/shortcut-help.tsx` plus `lib/keyboard/shortcuts.ts` own the shortcut help panel and registry-driven shortcut list.                                                                                                              | No exact import refs and no `ShortcutsHelpPanel` symbol refs outside the candidate.                                                            |
| `components/ui/shortcut-hint.tsx`              | Deleted  | `components/keyboard/shortcut-help.tsx` owns shortcut key rendering from the canonical shortcut registry.                                                                                                                                            | No exact import refs and no `ShortcutHint` symbol refs outside the candidate.                                                                  |
| `components/ui/status-badge.tsx`               | Deleted  | `components/ui/badge.tsx` owns generic badge variants. Domain owners such as `components/events/event-status-badge.tsx`, `components/inquiries/inquiry-status-badge.tsx`, and `components/quotes/quote-status-badge.tsx` own status-specific labels. | No exact import refs. `StatusBadge` symbol matches are local functions in feature files, not imports of this UI file.                          |
| `components/ui/status-dot.tsx`                 | Deleted  | Status dots are owned by domain-local indicators, with AI availability owned by `components/dashboard/ai-status-dot.tsx`.                                                                                                                            | No exact import refs. `StatusDot` symbol matches are local functions or unrelated DOM ids, not imports of this UI file.                        |
| `components/ui/determinate-progress.tsx`       | Deleted  | `components/ui/context-loader.tsx` owns loading progress when real progress exists, and `components/ui/state-motion.tsx` owns progress fill motion.                                                                                                  | No exact import refs and no `DeterminateProgress` symbol refs outside the candidate. Historical session log mentions only prior touched files. |
| `components/ui/remy-loader.tsx`                | Deleted  | `components/ui/context-loader.tsx` owns the Remy visual through registry-backed loading contexts. `components/ui/loading-state.tsx` owns generic centered loading.                                                                                   | No exact import refs. `RemyLoader` symbol refs are audit prose only.                                                                           |
| `components/ui/rich-note-editor.tsx`           | Retained | `components/ui/tiptap-editor.tsx` is the replacement owner, but `docs/specs/rich-text-editor-dinner-circles.md` explicitly says not to delete this file.                                                                                             | No exact import refs and no `RichNoteEditor` symbol refs outside the candidate, but contrary deletion guidance blocks pruning in this slice.   |

## Validation Notes

- Exact import search used `rg --fixed-strings "@/components/ui/<file>" app components lib tests scripts docs`.
- Symbol search used targeted exported component names and excluded the candidate files.
- This pass intentionally did not touch recover, uncertain, or prune-candidate UI files.
