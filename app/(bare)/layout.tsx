// Bare layout: no header, no footer, no ChefFlow branding.
// Used for outreach landing pages where neutral presentation is required.

export default function BareLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
