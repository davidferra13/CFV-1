/**
 * Chef portal page template.
 * Next.js `template.tsx` re-mounts on every navigation.
 */
export default function ChefTemplate({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
