import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Culinary Profile | ChefFlow' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
