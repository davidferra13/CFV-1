import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'AI Privacy | ChefFlow' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
