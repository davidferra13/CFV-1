import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'New Service Log | ChefFlow' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
