#!/usr/bin/env node
/** Local export remains held until its destination and lineage are independently verified. */
console.error('Archive export is held pending protected destination verification. No files were written.')
process.exitCode = 2
